# Smithery MCP registry manifest — https://smithery.ai
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({ command: "evalgate-mcp", args: [] })
